**Design a Contact Form**

Build an app that is functionally similar to this example project. Try not to copy the example project, give it your own personal style.



**Objective:** Fulfill the user stories below and get all the tests to pass to complete the lab.



**User Stories:**



1. You should have a div with a class of form-container to hold your form.
2. You should have a form element within the .form-container element.
3. Within the form element, you should have the following elements and input fields:
   An h2 element with some text.
   A text input field for the name with the type set to text and id, name, required attributes.
   An email input field with the type set to email and id, name, required attributes.
   A textarea for the message with id, name, and the required attribute.
   A button element with the text "Submit" and a type attribute of submit.
4. Each input and textarea elements should have their corresponding label element with a for attribute set to the element's id.
5. The .form-container element should have a background color, and have values for border-radius, padding, and width.
6. The label elements should have a margin and a font color.
7. The input and textarea elements should have values for:

   . width.
   . padding.
   . Margin on the bottom.
   
8. The button element should have a:

   . Background color
   . Font size apart from the default.
   
9. The button should have a hover effect that changes the background color.



**Note:** Be sure to link your stylesheet in your HTML and apply your CSS.





Tests

Passed:1. You should have a div with a class of form-container.

Passed:2. You should have a form element within the .form-container element.

Passed:3. Within the form, you should have an h2 element with some text.

Passed:4. Within the form, you should have a text input field with the type set to text.

Passed:5. Your type=text input field should have an id, a name, and a required attribute.

Passed:6. Within the form, you should have an email input field with the type set to email.

Passed:7. Your type=email input field should have an id, a name, and a required attribute.

Passed:8. Within the form, you should have a textarea with an id, name, and a required attribute.

Passed:9. Within the form, you should have a button element with a type attribute of submit.

Passed:10. Your button should have a text of "Submit".

Passed:11. You should have a label element for the #name element with a for attribute matching the id value on the corresponding input.

Passed:12. You should have a label element for the #email element with a for attribute matching the id value on the corresponding input.

Passed:13. You should have a label element for the #message element with a for attribute matching the id value on the textarea.

Passed:14. The labels shouldn't be empty.

Passed:15. The .form-container element should have a background color.

Passed:16. The .form-container element should have a border-radius.

Passed:17. The .form-container element should have padding.

Passed:18. The .form-container element should have a width.

Passed:19. Your labels should have a margin and font color.

Passed:20. Your input elements should have a width, padding, and a margin on the bottom.

Passed:21. Your textarea element should have a width, padding, margin on the bottom.

Passed:22. Your button element should have a background color.

Passed:23. Your button element should have a font size.

Passed:24. Your button element should have a hover effect that changes the background color.

